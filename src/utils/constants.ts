// Urls
export const BASE_URL = process.env.REACT_APP_API_URL;

// Date formats
export const TIME_FORMAT = 'HH:mm';
export const DATE_FORMAT = "DD.MM.YYYY";
export const DATE_TIME_FORMAT = "DD.MM.YYYY HH:mm";

// Button style
export const BUTTON_SIZE: "large" | "middle" | "small" = "middle";
export const BUTTON_TYPE: "default" | "dashed" | "link" | "primary" | "text" = "default";

// Table style
export const TABLE_MIN_HEIGHT: number = 200;